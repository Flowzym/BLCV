export function analyzeTextContent(text:string){const words=(text||'').toLowerCase().split(/\W+/).filter(Boolean); const freq:Record<string,number>={}; for(const w of words) freq[w]=(freq[w]||0)+1; return { keywords:Object.entries(freq).sort((a,b)=>b[1]-a[1]).slice(0,10).map(([w])=>w), sentiment:'neutral' as const };}
